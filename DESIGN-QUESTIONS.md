# Awesome Deep Research Papers — 设计确认

请按顺序填写每个“你的回答”区域。可以直接修改本文件，也可以在对话中按问题编号回复。

## 已确认

### Q1：仓库方式

- 决定：新建仓库。
- 仓库名：`awesome-openqa-papers`。
- 可见性：公开。

### Q2：入库门禁

- 决定：候选论文先生成 Draft PR，由用户确认后合并。

### Q3：模型运行方式

- 决定：使用桌面 Codex Scheduled Task 和现有 Codex 使用额度。
- 约束：定时运行时 Mac 需要开机，ChatGPT/Codex App 需要运行。
- 不在首版中接入独立模型 API。

### Q4：Seed 初始数据

- 决定：先搭建空框架，之后再提供 Seed 论文。

### Q5：首版输出

- 决定：只生成 GitHub Markdown。
- 主要文件：`README.md` 和 `papers/<arxiv-id>.md`。
- 静态网站延后。

### Q6：每周处理上限

- 决定：最多审核 30 篇摘要，最多精读 5 篇全文。
- 这是处理上限，不是收录目标；最终可以收录 0–5 篇。
- 没有合格论文时不创建 PR。

### Q7：任务隔离

- 决定：Scheduled Task 使用专用 Git worktree，不修改日常主 checkout。

### Q8：GitHub 发布

- 决定：本地验证完成后再创建并连接公开 GitHub 仓库。

## 第一轮回答

### Q11：OpenQA 的范围

需要明确 `OpenQA` 的含义和边界。

推荐定义：

> OpenQA 指开放域问答，包括检索增强问答、Web QA、多跳问答和搜索 Agent。排除纯闭卷问答，以及方法不能推广到开放域场景的单一行业问答。


```text
你的回答：openqa指的是DeepResearch场景，尤其是多模态deepresearch。仓库应该包含三大模块，1，关于deepresearch的数据合成，尤其是带附件输入或者要求图文并茂输出的；2，rubrci-rl，针对此类开放式任务设计rubric-rl算法的，重点关注rubric的自演进合成和优化，以及rubrics最终标量计算优势，忽略了特定critia的满足与否，3.harness优化，强调在训练过程中，harness不再是静止不改变的基建，而是通过训练汲取经验修改harness或者skill
```

### Q12：精读笔记语言

推荐格式：

- 论文原标题保留英文。
- 精读笔记使用中文。
- 专业术语首次出现时保留英文。

你的回答：

```text
好的
```

### Q13：定时运行时间

推荐时间：每周一 `09:17`，时区 `Asia/Shanghai`。

你的回答：

```text
每周一、周四上午9点。这次单独运行一次
```

### Q14：缺少 Seed 时的行为

推荐规则：少于 3 篇正向 Seed 时拒绝正式定时运行，只允许本地测试，避免生成低质量 PR。

你的回答：

```text
好的，我会为每个方向都提供seed论文
```

### Q15：是否保存 PDF

推荐规则：不把 PDF 提交到仓库，只保存论文链接、结构化元数据和精读笔记。这样仓库更轻，也减少版权风险。

你的回答：

```text
好的
```

## 第一轮确认声明

全部问题回答后，请填写：

```text
我确认以上范围，可以开始实现：是
```

## 第二轮待确认

这些问题只决定“哪些论文算相关”。自动化、目录结构和运行方式已经确认，不再重复。

### Q16：仓库名称

当前主题是 Deep Research，而 `OpenQA` 通常会被理解成开放域问答，可能让读者和自动筛选产生误解。

推荐：仓库改名为 `awesome-deep-research-papers`。如果你希望保留 `awesome-openqa-papers`，请说明 `OpenQA` 是有意使用的上位品牌。

你的回答：

```text
按照推荐来
```

### Q17：三大方向如何归类

一篇论文可能同时涉及数据合成、Rubric RL 和 Harness 优化。

推荐：允许多标签，但只指定一个主方向；README 只展示一次，避免重复收录。

你的回答：

```text
好的
```

### Q18：Deep Research 数据合成的边界

推荐：收录明确服务于 Deep Research／开放式研究任务的数据合成，以及能直接迁移到“附件输入”或“图文交错输出”的方法；排除只做通用图文增强、普通 VQA 或合成 QA 的论文。

你的回答：

```text
好的
```

### Q19：Rubric RL 的边界

你描述了两个重点：

1. Rubric 的自动生成、自演进、合成和优化；
2. 多条 criteria 聚合成单一标量奖励或优势后，单项满足情况被掩盖的问题。

推荐：两类都收录，分别标记为 `rubric-evolution` 和 `criterion-credit`，并优先精读第二类。

你的回答：

```text
好的，稍后我会整理一批各方向下的seed论文
```

### Q20：Harness 优化的定义

推荐定义：Harness 是模型权重之外的任务执行层，包括 system prompt、tools、skills、workflow、retrieval 和 agent scaffold。只收录 Harness 会从训练或执行经验中被自动修改、选择或优化的工作；排除静态工具框架和普通 Agent 工程。

你的回答：

```text
对
```

### Q21：Seed 的最低要求

推荐：三个主方向各至少提供 1 篇 Seed，共至少 3 篇。某方向尚无 Seed 时，只暂停该方向，不影响其他方向运行。

你的回答：

```text
好
```

### Q22：第一次手动运行的时间范围

推荐：Seed 提供后，第一次只回溯最近 30 天，用来校准筛选结果；确认准确后，再单独做历史论文回填。

你的回答：

```text
好的，也可以重点查看seed论文里的相关工作和引用seed的论文
```

## 最终确认声明

回答 Q16–Q22 后，请填写：

```text
我确认全部设计，可以开始实现：是
```
