# 从 nxc 微信聊天记录收录资料

使用 [Dukou](https://github.com/qzz0518/Dukou) 将微信主动导出的记录粘贴到本项目的 Codex 任务。论文与有价值的非论文链接都按 [微信手动收录协议](CHAT-CODEX.md) 处理。

## 配置

- 微信需要支持“转发到其他应用”（Dukou 文档要求 4.1.13 或以上）。本机新版安装路径为 `/Users/chenxn2020/Applications/WeChat.app`；`/Applications/WeChat.app` 仍是旧版 3.8.8。
- Dukou 0.1.8 安装在 `/Applications/Dukou.app`。
- 在 Dukou 的“入口”启用“发给 Codex”。自动粘贴和快捷微信转发需要 Dukou 的辅助功能权限。
- 使用“快捷微信转发”时，聊天名称填写 `nxc`，目标选择正在运行的 Codex（本机可能显示为 ChatGPT），粘贴方式选择“粘贴文件”。首次测试使用最近 1 条，之后按本次需要调整。
- Dukou 的输入框虽然标为“微信群名称”，0.1.8 的匹配逻辑按会话名称查找，并未要求会话必须是群；nxc 联系人已通过原生“转发到其他应用”导出验证，快捷转发仍需单独验证。
- 在“入口”页启用“附加 Prompt”并选中下面的提示词；“快捷微信转发”页有独立的启用开关，两页现均已开启。目标应用并不等于固定项目；转发前先切换到本仓库的 Codex 任务并点选输入框。

## 附加 Prompt

```text
请收录本次附带的 nxc 微信聊天记录中的论文和有价值的非论文链接。目标仓库：/Users/chenxn2020/Desktop/projects/awesome-deep-research-papers。先读取该仓库的 AGENTS.md 和 CHAT-CODEX.md，按手动收录协议处理本次附件。用户已完成初筛：保留完整推送 URL、发现来源与原始资源 URL；先去重，再查原文完成中文精读，论文归入四个论文 Track，官方研究博客归入 Official Lab Blogs，其他资源归入 Others。按照 paper-template.md 的统一精读格式直接写入正式目录，不生成 reports/intake 草稿，不等待二次确认；正文不要加入 ZIP 清单、转发过程或无关核对日志。若导出内容缺少可唯一核实的原文，回复并标记“待补原文”，不要写正式条目，也不要把二手推送当作全文精读。
```

## 平时使用

1. 在手机上照常把公众号和小红书资料分享给自己 `nxc`，等待消息同步到新版 Mac 微信。
2. 在 Codex 打开本仓库的任务，点选输入框。
3. 在 Mac 微信的 nxc 会话中多选本次资料，使用合并转发里的“转发到其他应用”，选择“发给 Codex”。这条路径不需要截图。“快捷微信转发”在本机尚未成功识别 nxc，暂不作为已验证的入口。
4. 检查 Codex 输入框中的提示词和附件后发送。Dukou 只负责导入，不代表任务已经开始精读。
5. Codex 完成精读后直接写入正式目录；只有无法唯一核实原文的内容才不入库并标记“待补原文”。

## 首次验证

以最近 1 条作为样本：确认选中的是 nxc、导出的文本有消息标题或截图、附件进入本项目的 Codex 输入框。公众号卡片和小红书卡片分别验证；小红书可以只转发包含标题、作者、摘要或正文要点的截图，我会据此反查原文。只发截图时不保证能唯一定位，需标记“待补原文”；截图只用于本次定位和核对，不保存到 Git。原始导出包保留在 Dukou 管理的本地位置，仓库只保存整理后的 Markdown。

当前采用主动导入，不配置后台监听或定时扫描历史聊天。

## 本机验证进度（2026-09-09）

已安装并通过签名、公证及安装包校验：Dukou 0.1.8、新版微信 4.1.13（官方安装包版本 4.1.13.61）。Dukou 的“发给 Codex”入口已启用；原生分享与快捷微信转发均已启用上面的收录 Prompt。快捷转发配置为 nxc、最近 1 条、目标 ChatGPT、粘贴文件。

用户已确认新版微信登录并打开，且反馈截图时微信会隐藏内容；不能根据工具看到的白屏判断登录失败。已确认只运行新版微信，但 Dukou“读取当前群”仍报“没有找到唯一匹配的群聊”，原因尚未确定。

已完成 1 条公众号卡片的原生分享验证：用户从 nxc 导出 LoopArena 消息，通过 Dukou 将 ZIP 和收录 Prompt 发送到当前任务。ZIP 内有 1 个聊天文本文件，保留了消息标题、聊天时间及完整公众号 URL（含原始分享参数）；已据此找到对应论文和官方仓库。无需截图即可开始精读。

已完成 1 条小红书卡片的原生分享验证：用户从 nxc 导出 GEAR 消息，通过 Dukou 将 ZIP 和收录 Prompt 发送到当前任务；ZIP 文本保留了完整小红书 URL，且可据公开 arXiv 与官方仓库唯一核实原文。小红书截图-only 也已纳入协议：截图可用于反查，但无法唯一定位时必须标记“待补原文”，不把截图当作全文精读。“快捷微信转发”自动识别 nxc 仍待验证；当前电脑控制工具不允许操作 Codex 界面，输入框确认和发送需由用户完成。
